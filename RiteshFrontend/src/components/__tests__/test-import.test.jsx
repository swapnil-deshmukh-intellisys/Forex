import { describe, it, expect } from 'vitest';
import MarketNewsFeed from '../MarketNewsFeed';

describe('Import Test', () => {
  it('should import MarketNewsFeed component', () => {
    expect(MarketNewsFeed).toBeDefined();
  });
});
